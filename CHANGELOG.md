# 更新日志

## 0.5.4 — 2026-08-17

### 新增

- 新增 TMDB 目录源：随仓库提供 decotv-tmdb-sidecar 目录代理服务，与 DecoTV 服务端同机部署（默认端口 4001，附 Docker compose 配置）。设置页可切换目录提供方，浏览与搜索的 11 个分类页均适配 TMDB 榜单与筛选；TMDB 不可用时自动回退豆瓣并提示。图片经 Luna 服务代理投递，海报加载不经过 WebView。
- TMDB sidecar 地址自动推导：默认与 DecoTV 服务器同主机、端口 4001，无需手动输入；地址显示在服务器信息中。
- 大分类自动下加载：分类页焦点移到列表末尾约两行时自动获取下一页，最多追加 4 页，单分类最多 100 个条目。豆瓣与 TMDB 页大小统一为 20，两端翻页行为一致；追加渲染不重建网格，焦点与滚动位置不受影响。返回详情页后再回到分类页时按已加载页数恢复，不重复请求。

### 改进

- 首页各分行跟随目录提供方：切换到 TMDB 后首页热门行改用 TMDB 数据，与分类页一致。
- 详情页系列作品关联 badge 改由服务端 lifecycle sites 数据驱动：不再以标题首段猜测同系列作品，避免误匹配；修复同系列重复搜索的问题。
- TMDB 剧集分类排除动画：豆瓣剧集池与动漫分类分离，TMDB 侧通过排除动画 genre 保持一致，剧集页不再混入美漫日漫。

### 修复

- 修复首页分行首张卡片焦点环被裁切的问题。
- 修复从详情页返回搜索页时筛选条件与列表状态丢失的问题：现恢复离开前的筛选、列表、滚动位置与焦点。

### 打包与验证

- 新增 TMDB 客户端相关单元测试；分类自动下加载在真机上经 CDP 验证豆瓣与 TMDB 两端均可连续加载至 100 条封顶。

## 0.5.3 — 2026-08-13

### 新增

- 播放器新增重启按钮：控制栏可一键回到当前集开头，保留播放/暂停状态。
- 详情页新增系列作品关联 badge：以标题首段为关键词搜索，展示同系列相邻季/衍生作品，点击直接进入详情；结果按关键词缓存，重复访问不重新请求。

### 修复

- 修复片尾自动跳集只触发一次的问题：`_outroTriggered` 在换集/换源后未重置，导致同一播放会话内只有第一次片尾标记生效。

## 0.5.2 — 2026-08-06

### 修复

- 修复换源后概率性从头（0 秒）重播的问题：连续 failover 时 `handlePlaybackError` 读取的 `video.currentTime` 已被 `video.load()` 重置为 0，导致丢失原始进度；现回退到上一次换源设置的 `resumeTime`，续播位置在整条换源链路中保持。
- 绕过服务端 m3u8 广告过滤代理：所有 `/api/` 请求追加 `client=orion`，使服务端按原生 TV 客户端处理，保留 webOS starfish 在广告拼接点所需的 `EXT-X-DISCONTINUITY` 标记（对应 DecoTV issue #227）；广告跳过仍由客户端内置扫描器完成。

## 0.5.1 — 2026-08-03

### 新增

- 设置页新增「退出登录」（仅在已登录时显示）：注销服务端会话并清除本机 Cookie，不再自动降级为匿名浏览，返回服务器地址页重新开始。
- 播放器长按快进快退：进度条聚焦时按住 ←/→ 平滑加速步进（8x 起步，最高约 96x），滑块上方气泡实时显示目标时间点，松手一次性定位，不再每次重复键都触发 seek。

### 改进

- 播放器交互模型重设计：进度条成为一等焦点对象（默认焦点）。进度条上短按 ←/→ = ±10 秒、长按进入平滑快进快退、OK 播放/暂停、↓ 进入按钮行；按钮行内 ←/→ 移动、↑ 返回进度条。原「↑ 打开剧集列表」快捷方式取消，剧集列表改用按钮行的「列表」按钮。控制条隐藏时按 ←/→ 直接唤出并开始快进快退。
- 登录门层级统一为「服务器地址 → 用户密码 → 首页」：退出登录回到服务器地址页（预填当前地址），用户密码页 Back 返回服务器地址页，服务器地址页 Back 退出应用。
- 切换服务器改为事务式：从设置进入服务器地址页不再立即清除会话，仅当在新服务器完成一次登录（密码或匿名）后切换才生效；中途 Back 恢复原会话；连接新地址失败时保留原服务器配置，地址栏预填当前地址。
- 会话 Cookie 存储从 /media/internal 移入应用安装目录：卸载或重装即清除授权，不再出现重装后静默续期；升级重装后需重新登录一次。

### 修复

- 修复凭据丢失（如重装后）但服务端会话仍有效时，收藏与播放记录同步被静默禁用的问题。
- 修复硬件 Back 键在按键与 popstate 双路径下被重复分发、可能导致误退出应用的问题。
- 修复服务器地址页与登录页遥控器方向键无法移动焦点的问题（此前仅 Magic Remote 指针可用）。
- 修复控制条隐藏后残留按钮焦点造成的误操作（隐藏时清空焦点，唤出时焦点回到进度条）。

### 打包与验证

- 打包脚本与 CDP 调试工具迁移至共享子模块 [webos-tv-kit](https://github.com/CheerChen/webos-tv-kit)（`tvkit/`），构建改为薄包装脚本；克隆仓库后需执行 `git submodule update --init`。
- 新增服务器切换事务、退出登录、账号会话判定的单元测试。
- 播放器、详情、搜索三屏内部重构：测速优选引擎、播放控制器、OSD、侧面板、浏览配置拆分为独立模块并补充单元测试；对外行为与交互不变。
- 新增基于 Playwright 的遥控器路径 E2E 测试套件（`tests/e2e`），以真实按键路径固定关键用例行为。

## 0.5.0 — 2026-08-02

### 新增

- 支持 password 账号模式，0.4.2 仅支持 public 模式。IPK 内置 webOS JS Service，在 WebView 外发起 API 请求并保存会话 Cookie，登录一次后自动恢复会话。
- 账号模式下收藏和播放记录与服务端双向同步，跨设备一致；public 模式或未登录时继续使用电视本地存储。
- 详情页对全部播放源进行并发测速，按画质、吞吐和启动时延选择播放源；测速在后台继续完成，期间可随时用当前实测最优源开始播放。
- 播放失败时从剩余播放源中选择实测结果最佳的源，并从中断位置继续播放；播放器内支持手动换源。
- 增加海报加载管线：Service 请求、Blob URL、缓存、失败回退、并发去重和可信域校验。
- 增加 HLS 中插广告识别与跳过：预扫描分片分辨率，进入广告区间时一次性跳过。
- 播放器增加上一集、下一集按钮，频道 +/− 键同样可切换；方向键 ↑ 打开剧集列表。
- 增加片尾标记和自动连播：在播放器标记一次片尾，本剧各集播放到该位置自动进入下一集（最后一集除外）；标记按距结尾的偏移记录，各集时长不同也能对齐，仅保存在本机。
- 详情页点击播放源直接以该源播放；播放按钮显示继续播放集数，上次观看的剧集高亮标记，收藏状态实时更新。
- 首页展示热门电影、剧集、动漫和综艺；数字键 `0–9` 可快速切换导航。

### 改进

- 播放条、剧集列表和换源面板共用 5 秒无操作自动隐藏计时器，暂停时保持显示。
- 详情页动作区仅保留「播放」与「收藏」；「重新测速」位于播放源区块，返回操作由遥控器 Back 键承担。
- 播放器控制条按 ⏮ ⏯ ⏭ 顺序排列；「标记片尾」置于末位，标记状态显示在进度条上（触发位置刻度与片尾区域着色）。
- 统一焦点表达：实心高亮仅表示焦点，按钮重绘不再丢失焦点位置。
- 更新 public 模式、账号模式、凭据保存和会话限制的中英文说明。

### 修复

- 修复过期 Cookie 或 public 模式登录响应被误判为有效会话的问题。
- 修复服务器切换时复用其他服务器凭据的问题。
- 修复密码模式下海报无法加载的问题。
- 修复播放源测速未真正参与选择、播放失败后按列表顺序盲切的问题。
- 修复首页内容更新和本地库同步时焦点丢失的问题。

### 打包与验证

- JS Service 作为独立服务打入 IPK，并增加包内容清单、包体大小和发布 SHA256 校验。
- 增加 CDP 调试、Telnet Luna、网络诊断和电视冒烟测试脚本。
- 增加认证、会话、图片缓存、图片管线、库同步、播放源排序、广告跳过和 TS 分辨率测试。
