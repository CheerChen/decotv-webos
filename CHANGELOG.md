# 更新日志

## 0.5.0 — 2026-08-02

### 新增

- 支持 password 账号模式，0.4.2 仅支持 public 模式。IPK 内置 webOS JS Service，在 WebView 外发起 API 请求并保存会话 Cookie，登录一次后自动恢复会话。
- 账号模式下收藏和播放记录与服务端双向同步，跨设备一致；public 模式或未登录时继续使用电视本地存储。
- 详情页对全部播放源进行并发测速，按画质、吞吐和启动时延选择播放源；测速在后台继续完成，期间可随时用当前实测最优源开始播放。
- 播放失败时从剩余播放源中选择实测结果最佳的源，并从中断位置继续播放；播放器内支持手动换源。
- 增加海报加载管线：Service 请求、Blob URL、缓存、失败回退、并发去重和可信域校验。
- 增加 HLS 中插广告识别与跳过：预扫描分片分辨率，进入广告区间时一次性跳过。
- 播放器增加上一集、下一集按钮，频道 +/− 键同样可切换；方向键 ↑ 打开剧集列表。
- 增加片尾标记和自动连播：在播放器标记一次片尾，本剧各集播放到该位置自动进入下一集（最后一集除外）；标记按距结尾的偏移记录，各集时长不同也能对齐，仅保存在本机。
- 详情页点击播放源直接以该源播放；播放按钮显示继续播放集数，上次观看的剧集高亮标记，收藏状态实时更新。
- 首页展示热门电影、剧集、动漫和综艺；数字键 `0–9` 可快速切换导航。

### 改进

- 播放条、剧集列表和换源面板共用 5 秒无操作自动隐藏计时器，暂停时保持显示。
- 详情页动作区仅保留「播放」与「收藏」；「重新测速」位于播放源区块，返回操作由遥控器 Back 键承担。
- 播放器控制条按 ⏮ ⏯ ⏭ 顺序排列；「标记片尾」置于末位，标记状态显示在进度条上（触发位置刻度与片尾区域着色）。
- 统一焦点表达：实心高亮仅表示焦点，按钮重绘不再丢失焦点位置。
- 更新 public 模式、账号模式、凭据保存和会话限制的中英文说明。

### 修复

- 修复过期 Cookie 或 public 模式登录响应被误判为有效会话的问题。
- 修复服务器切换时复用其他服务器凭据的问题。
- 修复密码模式下海报无法加载的问题。
- 修复播放源测速未真正参与选择、播放失败后按列表顺序盲切的问题。
- 修复首页内容更新和本地库同步时焦点丢失的问题。

### 打包与验证

- JS Service 作为独立服务打入 IPK，并增加包内容清单、包体大小和发布 SHA256 校验。
- 增加 CDP 调试、Telnet Luna、网络诊断和电视冒烟测试脚本。
- 增加认证、会话、图片缓存、图片管线、库同步、播放源排序、广告跳过和 TS 分辨率测试。
